// Здравствуйте! У меня вопрос: Моя специализация Управляюзий проектами. Программирование, даже в файле ниже,
// дается с трудом. Это не то чтобы сложно, но требует много времени. Если я правильно понял, то ответом на
// все мои вопросы будет - Так эта функция работает.

void FillArray(int[] collection)     // неясно как присвоилось имя collection? или это просто для внуттреннего пользования этой функции?
{                                    // FillArray(array);из строки 49 и void вроде как вызвали функцию
                                    // в строке 5 и параметры подтянулись из строки 47?
   
    int length = collection.Length;  // Массив с именем collection принял условие из строки 47?
    int index = 0;                      
    while (index < length)           
    {
        collection[index] = new Random().Next(1, 10);
        //index = index + 1;
        index++;
    }
}

void PrintArray(int[] col)    // то же, что и в заметке на строках 5-9?
{
    int count = col.Length; 
    int position = 0;
    while(position < count)
    {
        Console.WriteLine(col[position]);
        position++;
    }
}

int IndexOf(int[] collection, int find) // имеется в виду- функция IndexOf (массив в котором ищем, зпеременная которую ищем)?
{                                       // и функция сама поняла, что find это 4 из строки 53, а collection это array из строки 53?
    int count = collection.Length;
    int index = 0;
    int position = -1;
    while (index < count)
    {
        if(collection[index] == find)  // Переменная find объявлена в строке 53 (цифра 4)?
        {
            position = index;
            break;
        }
        index++;
    } 
     return position;
}

int[] array = new int [10]; // int - это тип данных, array - имя,  new int - объявляем массив из 10 элементов. 

FillArray(array);           // выполнить функцию FillArray(имя массива)?
PrintArray(array);          // выполнить функцию PrintArray(имя массива)?
Console.WriteLine();        // написать в консоли результат выполнения команд  из строк 49 и 50?

int pos = IndexOf(array, 4);  // номер позиции = Функция IndexOf (массив в котором ищем, значение котор ищем)?
                              //
Console.WriteLine(pos);       // тут понял

// То есть получается, что мы сперва подготовили функции, а потом их повызывали и дали им условия?


// Я понимаю, что все дается в очень скомканном виде чтобы я сам искал информацию, но мне программирование не по душе)
// На сколько при моей специализации мне это программирование необходимо и как скоро оно у меня закончится?
// Пока я справляюсь вытаскиванием кусков кода из разных уроков и методом их перебора, и надеюсь с таким подходом преодолеть то,
// что заложено в учебном плане по моей специализации.